# Nanoagent Level 2 - local configuration
#
# Fill these in to run the agent against an OpenAI-compatible endpoint.
# (For Azure OpenAI with the OpenAI-compatible API, your api base often ends with /openai/v1/)

# Required (unless you set OPENAI_API_KEY instead)
NANOAGENT_API_KEY=

# Required (unless you set OPENAI_API_BASE instead)
NANOAGENT_API_BASE=

# Optional
NANOAGENT_MODEL=gpt-4.1-mini
